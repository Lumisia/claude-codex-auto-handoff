node_modules/
*.log
.DS_Store
/tmp/
.claude/
.env
.env.*
*.bak
*.backup
*.orig

# Rust build artifacts (v2 cargo workspace)
/target/
**/target/

# legacy/root-level Node test suite is not shipped in v2
/tests/

# Tauri desktop frontend build output + generated schemas
apps/desktop/dist/
apps/desktop/src-tauri/gen/

# local runtime store + generated reports (per-machine, not source)
.ai-handoff-store/
ai-handoff-hooks-report.md

# local investigation scratch clones
.tmp-*/

# subagent-driven-development scratch (briefs, reports, diffs, ledger)
.superpowers/

# internal planning docs (specs/plans) — not shipped to users
docs/superpowers/
