---
description: Show whether a handoff capsule is pending
---

Use the handoff-session skill to run `handoff:status` for the current project
and report whether a capsule is pending (and its delivery state).
