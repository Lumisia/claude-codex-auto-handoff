{
  "name": "ai-handoff",
  "version": "1.1.15",
  "description": "Context-pressure-aware handoff between Claude Code and Codex",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "validate:package": "node scripts/validate-package.mjs"
  }
}
